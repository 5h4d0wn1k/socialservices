import React from 'react';

const ProgressChart = () => {
  // This is a placeholder component that would normally contain a chart library
  // You might want to use a library like recharts or chart.js
  return (
    <div className="h-64 bg-gray-50 rounded-lg flex items-center justify-center">
      <p className="text-gray-500">Volunteer hours chart will be displayed here</p>
    </div>
  );
};

export default ProgressChart;